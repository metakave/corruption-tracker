#!/usr/bin/expect

set timeout 30
set ip "46.224.92.166"
set user "root"
set password "qR0h7fHOfn/kBwoKfOAvVg=="

# Check for audit dashboard files
spawn ssh -o StrictHostKeyChecking=no $user@$ip "ls -la /var/www/political_violence_tracker/app/audit-dashboard"
expect {
    "password:" { send "$password\r" }
}
expect eof
