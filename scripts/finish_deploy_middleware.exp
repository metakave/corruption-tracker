#!/usr/bin/expect

set timeout 30
set ip "46.224.92.166"
set user "root"
set password "qR0h7fHOfn/kBwoKfOAvVg=="

# Run restart and crawler
spawn ssh -o StrictHostKeyChecking=no $user@$ip "cd /var/www/political_violence_tracker && pm2 restart all"
expect {
    "password:" { send "$password\r" }
}
expect eof
