#!/usr/bin/expect

set timeout 30
set ip "46.224.92.166"
set user "root"
set password "qR0h7fHOfn/kBwoKfOAvVg=="
set remote_path "/var/www/political_violence_tracker"

spawn scp -o StrictHostKeyChecking=no lib/ai-analysis.ts $user@$ip:$remote_path/lib/ai-analysis.ts
expect {
    "password:" { send "$password\r" }
}
expect eof

# Restart app to load new prompt (since it's in a lib, but processed by worker/Next)
spawn ssh -o StrictHostKeyChecking=no $user@$ip
expect {
    "password:" { send "$password\r" }
}
expect "#"
send "cd $remote_path && npm run build && pm2 restart all\r"
expect "#"
send "exit\r"
expect eof
