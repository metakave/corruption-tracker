#!/usr/bin/expect

set timeout 30
set ip "46.224.92.166"
set user "root"
set password "qR0h7fHOfn/kBwoKfOAvVg=="

spawn ssh -o StrictHostKeyChecking=no $user@$ip "pm2 start 0 && pm2 save && pm2 list"
expect {
    "password:" { send "$password\r" }
}
expect eof
