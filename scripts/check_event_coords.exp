#!/usr/bin/expect

set timeout 30
set ip "46.224.92.166"
set user "root"
set password "qR0h7fHOfn/kBwoKfOAvVg=="

spawn ssh -o StrictHostKeyChecking=no $user@$ip "export DATABASE_URL='postgresql://political_violence_user:password123@localhost:5432/political_violence_tracker?schema=public' && /var/www/political_violence_tracker/node_modules/.bin/prisma db push --schema /var/www/political_violence_tracker/prisma/schema.prisma --accept-data-loss --force-reset && echo 'Skipping reset' || echo 'Using script instead'; /var/www/political_violence_tracker/node_modules/.bin/ts-node --skip-project /var/www/political_violence_tracker/scripts/inspect_event_data.ts"
# Actually, I should just create a small script to query by title pattern
# Let's write a dedicated query script remotely or just use a raw query if possible, but raw query via CLI is hard.
# I will use the existing inspect_event_data.ts if it exists, or create a new one.
# I'll create a new one locally `scripts/check_coordinates.ts` and upload it.
expect {
    "password:" { send "$password\r" }
}
expect eof
