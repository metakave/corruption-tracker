#!/usr/bin/expect

set timeout 30
set ip "46.224.92.166"
set user "root"
set password "qR0h7fHOfn/kBwoKfOAvVg=="

# Check Nginx config
spawn ssh -o StrictHostKeyChecking=no $user@$ip "cat /etc/nginx/sites-enabled/default"
expect {
    "password:" { send "$password\r" }
}
expect eof
